{
  "name": "edunova_x",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run dev --prefix frontend\" \"npm start --prefix server\"",
    "start:ngrok": "npm run build --prefix frontend && npm start --prefix server",
    "install-all": "(cd frontend && npm install) && (cd server && npm install) && (cd signaling && npm install)"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "mongoose": "^8.19.1",
    "socket.io": "^4.8.1",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
