{
  "name": "edunova-x",
  "version": "1.0.0",
  "bin": {
    "edunova-x": "./bin/cli.js"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "build:frontend": "npm run build --prefix frontend",
    "pages:build": "node scripts/build.js",
    "cloudflare:build": "node scripts/build.js",
    "deploy:cloudflare": "npx wrangler deploy",
    "start": "npm run start:server",
    "start:server": "npm start --prefix server",
    "start:dev": "concurrently -n \"frontend,server,ai\" -c \"cyan,green,magenta\" \"npm run dev --prefix frontend -- --host 0.0.0.0\" \"npm start --prefix server\" \"python -m uvicorn ai_engine.main:app --host 0.0.0.0 --port 8001\"",
    "start:web": "concurrently -n \"frontend,server\" -c \"cyan,green\" \"npm run dev --prefix frontend -- --host 0.0.0.0\" \"npm start --prefix server\"",
    "start:ngrok": "npm run build --prefix frontend && npm start --prefix server",
    "install-ai": "python -m pip install -r ai_engine/requirements.txt",
    "install-all": "npm install --prefix frontend && npm install --prefix server && npm install --prefix signaling",
    "desktop:install": "npm install",
    "desktop:build": "npm run build && electron-builder --config electron-builder.yml",
    "desktop:win": "npm run desktop:build -- --win",
    "desktop:mac": "npm run desktop:build -- --mac",
    "desktop:linux": "npm run desktop:build -- --linux"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "mongoose": "^8.19.1",
    "socket.io": "^4.8.1",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "electron": "^28.2.0",
    "electron-builder": "^24.9.2"
  },
  "description": "EduNova X desktop installer and launcher",
  "author": "EduNova X",
  "main": "electron/main.js"
}
