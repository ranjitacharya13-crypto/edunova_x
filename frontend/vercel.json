{
  "//": "Vercel is NOT the production host. Production frontend = Cloudflare Workers (https://edunova-x.ranjitacharya13.workers.dev). This file is kept only for optional preview deploys. Backend URLs are intentionally NOT hardcoded here: set VITE_API_URL and VITE_SIGNAL_URL in the hosting dashboard so a stale URL can never be baked into a build.",
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/((?!assets/|.*\\..*).*)",
      "destination": "/index.html"
    }
  ]
}
