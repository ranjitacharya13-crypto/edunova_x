# Skip downloading the Electron prebuilt binary during `npm install`.
# The root package.json only needs the `electron` package for the desktop
# launcher; electron-builder downloads its own Electron dist at packaging time.
# This keeps `npm install` fast and reliable on CI / Cloudflare / Render,
# where binary downloads from github.com are restricted or slow.
electron_skip_binary_download=true
