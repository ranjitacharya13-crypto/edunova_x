# EduNova AI ORCHESTRATOR (Layer A) — lightweight, NO model runtime.
# This process never imports llama_cpp or torch; it talks to the inference
# service over HTTP. Keeping torch/llama out of this container is what lets
# it run comfortably inside 512 MiB.
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
pydantic>=2.8.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0
httpx>=0.27.0,<1.0.0
beautifulsoup4>=4.12.0,<5.0.0
jsonschema>=4.23.0,<5.0.0
