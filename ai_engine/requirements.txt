fastapi>=0.100.0
uvicorn>=0.22.0
pymongo>=4.0.0
pydantic>=2.0.0
bson>=0.5.10
# ml_model.py loads the trained intent model + vectorizer (ai_engine/models/*.pkl)
# and trains LinearRegression predictors; keep these available for the AI engine.
joblib>=1.3.0
scikit-learn>=1.3.0
