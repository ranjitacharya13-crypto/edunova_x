# Self-contained runtime matrix: even a provider that only runs `pip -r` must
# select CPU torch, never pull CUDA transitively through accelerate.
--extra-index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
--prefer-binary
llama-cpp-python==0.3.35
torch==2.8.0+cpu ; platform_system == "Linux" or platform_system == "Windows"
torch==2.8.0 ; platform_system == "Darwin"
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
pydantic>=2.8.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0
httpx>=0.27.0,<1.0.0
beautifulsoup4>=4.12.0,<5.0.0
jsonschema>=4.23.0,<5.0.0
# >=2.6 torch + safetensors only: do not downgrade transformers to enable
# vulnerable pickle checkpoints. Linux/Windows resolve the explicit +cpu build.
transformers==4.57.6
safetensors>=0.5.3,<1.0.0
accelerate>=1.0.0,<2.0.0
sentencepiece>=0.2.0,<1.0.0
tokenizers>=0.22.0,<0.23.0
huggingface-hub>=0.34.0,<1.0.0
